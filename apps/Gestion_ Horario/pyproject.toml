# pyproject.toml
[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gestion-horarios"
version = "1.0.0"
description = "Backend gestión horarios practicantes"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]